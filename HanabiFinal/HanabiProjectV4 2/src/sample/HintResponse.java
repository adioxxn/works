package sample;

/**
 * A class used to contain the servers
 * messages when we give a hint.
 */
public class HintResponse {
    boolean invalid;
    public HintResponse(boolean invalid) {
            this.invalid = invalid;
        }

}

